By uploading a .jsonl log file (CPU, GPU, Memory, IO, Context Switches data), the application automatically generates multiple real-time charts and graphs, making performance monitoring and benchmarking easier than ever.
